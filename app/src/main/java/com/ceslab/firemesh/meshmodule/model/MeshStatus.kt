package com.ceslab.firemesh.meshmodule.model

enum class MeshStatus {
    BLUETOOTH_STATE_CHANGED,
    LOCATION_STATE_CHANGED,
    MESH_CONNECTING,
    MESH_CONNECTED,
    MESH_DISCONNECTED,
    INIT_CONFIGURATION_LOADED

}